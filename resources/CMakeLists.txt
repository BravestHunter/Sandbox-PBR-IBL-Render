set(RESOURCE_DIR "resources")
set(SHADERS_DIR "${RESOURCE_DIR}/shaders" PARENT_SCOPE)
set(MODELS_DIR "${RESOURCE_DIR}/models" PARENT_SCOPE)
set(SKYBOXES_DIR "${RESOURCE_DIR}/skyboxes" PARENT_SCOPE)

install(
	DIRECTORY 
		"${PROJECT_SOURCE_DIR}/resources"
	DESTINATION
		.
)
